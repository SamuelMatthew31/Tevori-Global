backend:
  name: github
  repo: SamuelMatthew31/Tevori-Global
  branch: main
  base_url: https://tevori-global.vercel.app
  auth_endpoint: api/auth

# Allows direct editing on localhost without login via "npx decap-server"
local_backend: true

# Folder where Decap CMS saves uploaded media files (relative to git repository root)
media_folder: "public/images/uploads"
# Public URL path the browser uses to resolve images
public_folder: "/images/uploads"

collections: []
